#include "PuzzleGen.h"
#include <iostream>
using namespace std;

void PuzzleGen::generate(Board& board) {
    // randomly divide the board into rectangles, and put one number in a random cell per rectangle
}