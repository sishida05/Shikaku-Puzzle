#include "Solver.h"
#include <iostream>
using namespace std;

bool Solver::isSolvable(Board& board) {
    // checks whether the puzzle has a single solution with no overlapping rectangles
    return true;
}