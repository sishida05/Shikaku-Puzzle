#include "Rectangle.h"
#include <iostream>
using namespace std;

Rectangle::Rectangle(int r, int c, int w, int h) {
    // rectangle constructor
}
int Rectangle::area() const {
     // get the area of the rectangle
     return 0;
}