#include "Input.h"
#include <iostream>
using namespace std;

void Input::getMove(int& row, int& col, int& width, int& height) {
    // takes an input from the user for a specified rectangle (easy to upgrade later)
}